interface:
  display_name: "Claude"
  short_description: "Ask Claude safely with routing, files, and resume"
  default_prompt: "Use $claude to ask Claude through the durable bridge. It selects Subrouter or a clean direct login, passes large prompts and files safely, and preserves resumable sessions."
